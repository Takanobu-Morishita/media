<% if current_user.following?(user) %>
  <td><%= button_to 'フォローを外す', user_relationships_path(user_id: user.id), method: :delete %></td>
<% else %>
  <td><%= button_to 'フォローする', user_relationships_path(user_id: user.id), method: :post %></td>
<% end %>