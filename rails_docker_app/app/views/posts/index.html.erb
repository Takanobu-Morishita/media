<!DOCTYPE html>
<html>
  <head></head>
  <body>
    <h1>投稿一覧</h1>
    <table>
      <tr>
        <th>タイトル</th>
        <th>ジャンル</th>
        <th>投稿日</th>
      </tr>
      <% @posts.each do |post| %>
        <tr>
          <td><%= link_to post.title, post_path(post) %></td>
          <td><%= post.genre %></td>
          <td><%= post.created_at %></td>
        </tr>
      <% end %>
    </table>
  </body>
</html>
