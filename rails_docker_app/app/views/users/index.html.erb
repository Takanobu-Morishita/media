<!DOCTYPE html>
<html>
  <head></head>
  <body>
    <br>
    <%= link_to "投稿一覧", posts_path, class: "navbar-item button is-warning is-light" %>
    <br>
    <%= link_to "ユーザ作成", new_user_path, class: "navbar-item button is-warning is-light" %>
    <h1>ユーザ一覧</h1>
    <table>
      <tr>
        <th>ユーザ名</th>
        <th>メールアドレス</th>
        <th>登録日</th>
        <th>更新日</th>
      </tr>
      <% @users.each do |user| %>
        <tr>
          <td><%= link_to user.username, user_path(user) %></td>
          <td><%= user.email %></td>
          <td><%= user.created_at.strftime('%Y-%m-%d') %></td>
          <td><%= user.updated_at.strftime('%Y-%m-%d') %></td>
        </tr>
      <% end %>
    </table>
  </body>
  <%= paginate @users %>
</html>
